// Available radius options in km
export const RADIUS_OPTIONS = [0.1, 0.5, 1, 3, 5, 10, 20] as const;

// Default values
export const DEFAULT_RADIUS = 0.1; // 100m
export const DEFAULT_FROM_CURRENCY = "USD";
export const DEFAULT_TO_CURRENCY = "EUR";

